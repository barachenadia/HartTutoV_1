<% if @user.errors.any? %>
  
    <h5><%= pluralize(@user.errors.count, "erreur") %>
        ont empêché d'enregistrer votre inscription</h5>
    <p>Merci de corriger ces champs :</p>
    <ul>
    <% @user.errors.full_messages.each do |msg| %>
      <li style = "color: red;"> <%= msg %> </li>
    <% end %>
    </ul>
  
<% end %>